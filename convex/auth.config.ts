const authConfig = {
    providers: [
        {
            domain: 'https://verified-feline-70.clerk.accounts.dev',
            applicationID: 'convex',
        }
    ]
};

export default authConfig;